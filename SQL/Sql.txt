
create database company_db;

use company_db;

create table skills(
	skill_id int primary key identity(1,1),
	skill_name varchar(100) not null,
	category varchar(50)
);

SELECT name 
FROM sys.tables;

create table employee(
	name varchar(100),
	email varchar(150),
	hire_date date,
	salary int,
	dept_id int,
	gender varchar(10)
);

insert into employee values ('Alice green','alice.green@company.com','2024-01-10',62000.00,4,'female');


insert into employee (name,email,hire_date,salary,dept_id,gender)
values
('bob','bob.green@company.com','2025-01-10',69000.00,3,'female'),
('sam','sam.green@company.com','2025-01-20',59000.00,3,'female'),
('vetri','vetri.green@company.com','2023-11-10',90000.00,2,'female'),
('akila','akila.green@company.com','2020-10-10',100000.00,5,'female'),
('amar','amar.green@company.com','2021-01-13',120000.00,5,'female');

create table department(
	dept_id int primary key,
	located varchar(50)
);

alter table department 
add dept_name varchar(100);

insert into department values(5,'boston','sales')

select * from employee;

select name as Name,salary as Salary from employee;

select * from employee
where hire_date > '2021-01-01';

select * from employee
where salary > 65000
order by salary desc;

insert into department values(2,'boston','manager');
insert into department values(10,'chennai','HR');

select distinct located from department;

alter table employee
add phone_number varchar(15);

update employee 
set salary = 75000
where name = 'vetri';

update employee
set gender = 'male'
where dept_id = 3 or dept_id = 4;

alter table employee 
drop column phone_number;

select * from employee
where salary between 70000 and 150000;

select * from employee
where name like 'a%';

select * from employee
where dept_id = 3 or dept_id = 5;

select * from employee
where email is not null;

select * from department
where located not in('new york','chiicago');

select * from employee
where hire_date like '2023%';

select sum(salary) from employee;

select avg(salary) from employee;

select max(salary) from employee;

select count(*) from employee
where dept_id = 5;

select min(salary) from employee;

select * from employee e
join department d on d.dept_id = e.dept_id
where d.dept_name is not null;

select * from department d
left join employee e on e.dept_id = d.dept_id;

select *,d.* from employee e
full join department d on d.dept_id = e.dept_id;

select * from employee e
join department d on d.dept_id = e.dept_id
where d.located = 'boston';

select * from department d
right join employee e on e.dept_id = d.dept_id
where d.located is null;

select CONCAT(name,' ',gender) as full_name from employee;

select UPPER(dept_name) as dept_name from department;

select LEFT(email,3) as threeChar from employee;

select ABS(salary) from employee;

select ROUND(salary,2) from employee;

select top 3 * from employee
order by hire_date desc;

select * from employee
order by name 
offset 3 rows 
fetch next 3 rows only;

SELECT name,salary,
CASE 
    WHEN salary >= 70000 THEN 'High'
    ELSE 'Low'
END AS salary_level
FROM Employee;

select name,salary,
case 
	when salary >= 70000 then 'high'
	when salary >= 65000 then 'medium'
	else 'small'
end as salary_part
from employee;

select d.dept_id,sum(e.salary) from employee e
join department d on d.dept_id = e.dept_id
group by d.dept_id;

select top 1 * from employee
order by len(name) desc;

select * from employee
where DATEDIFF(day,hire_date,getdate()) > 90;

delete from employee
where salary < 90000;

drop table skills;

1.Restore Database (GUI)
Open SSMS
Right-click Databases
Click Restore Database…
Select Device → click …
Click Add → choose the .bak file
Select the backup set
Click OK

2.backup
Right-click Databases
click task -> backup
Full -> OK

Database will be restored.

select * from skills;
select * from employee;
select * from department;